name: aidokon
description: AI-powered automated checkout platform
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2  # Updated to resolve conflict
  cloud_firestore: ^5.4.3
  google_ml_kit: ^0.16.3
  image_picker: ^1.0.7
  printing: ^5.12.0
  intl: ^0.19.0
  http: ^1.2.2  # For payment gateway integrations
  web_socket_channel: ^3.0.1  # For real-time theft alerts

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/bread.png
    - assets/images/bread.png
    - assets/images/bread.png